'use strict';

describe('myApp.scale module', function() {

  beforeEach(module('myApp.scale'));

  describe('scale controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var scaleCtrl = $controller('ScaleCtrl');
      expect(scaleCtrl).toBeDefined();
    }));

  });
});
